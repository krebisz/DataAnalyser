<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:DataVisualiser.UI">
    <Style TargetType="{x:Type ui:ChartTabHost}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:ChartTabHost}">
                    <DockPanel Background="{DynamicResource ThemeAppBackgroundBrush}">
                        <ui:MetricSelectionPanel x:Name="PART_SelectionPanel" DockPanel.Dock="Top" />

                        <ScrollViewer Margin="5"
                                      Background="{DynamicResource ThemeAppBackgroundBrush}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ContentPresenter Content="{TemplateBinding ChartContent}" />
                        </ScrollViewer>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
