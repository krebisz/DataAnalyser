<UserControl x:Class="DataVisualiser.UI.Charts.Controllers.DistributionChartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    <Border BorderBrush="#FF444444"
            BorderThickness="1"
            Margin="0,0,0,10">
        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <Border Background="#BB131313"
                    BorderBrush="#FF444444"
                    BorderThickness="1"
                    Padding="10,5">
                <StackPanel Orientation="Horizontal" Margin="20,5,10,0">
                    <TextBlock x:Name="ChartTitle"
                               FontWeight="Bold"
                               FontSize="14"
                               VerticalAlignment="Center" />
                    <Button x:Name="ChartToggleButton"
                            Content="Show"
                            Margin="10,0,0,0"
                            Padding="10,3"
                            Click="OnChartToggle" />
                </StackPanel>
            </Border>
            <StackPanel Orientation="Horizontal" Margin="40,5,10,0">
                <TextBlock Text="Display Mode:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />
                <RadioButton x:Name="FrequencyShadingRadio"
                             Content="Frequency Shading"
                             GroupName="DisplayModes"
                             IsChecked="True"
                             Checked="OnDisplayModeChanged" />
                <RadioButton x:Name="SimpleRangeRadio"
                             Content="Simple Range"
                             GroupName="DisplayModes"
                             Checked="OnDisplayModeChanged" />
                <TextBlock Text="Interval Count:"
                           VerticalAlignment="Center"
                           Margin="20,0,10,0" />
                <ComboBox x:Name="IntervalCountCombo"
                          Width="80"
                          VerticalAlignment="Center"
                          SelectionChanged="OnIntervalCountChanged">
                    <ComboBoxItem Content="3" Tag="3" />
                    <ComboBoxItem Content="5" Tag="5" />
                    <ComboBoxItem Content="10" Tag="10" />
                    <ComboBoxItem Content="25" Tag="25" IsSelected="True" />
                    <ComboBoxItem Content="50" Tag="50" />
                    <ComboBoxItem Content="100" Tag="100" />
                </ComboBox>
            </StackPanel>
            <StackPanel x:Name="ChartPanel" Orientation="Vertical">
                <lvc:CartesianChart x:Name="Chart"
                                    LegendLocation="Right"
                                    Zoom="X"
                                    Pan="X"
                                    Hoverable="True"
                                    Margin="20,5,10,20"
                                    MinHeight="400">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Time" />
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Value"
                                  ShowLabels="True" />
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
