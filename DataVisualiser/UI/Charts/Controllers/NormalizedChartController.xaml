<UserControl x:Class="DataVisualiser.UI.Charts.Controllers.NormalizedChartController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:controls="clr-namespace:DataVisualiser.UI.Charts.Controllers"
             xmlns:ui="clr-namespace:DataVisualiser.UI.Defaults"
             mc:Ignorable="d"
             d:DesignHeight="550"
             d:DesignWidth="900">
    <Grid x:Name="RootGrid">
        <controls:ChartPanelController x:Name="PanelController" />

        <StackPanel x:Name="BehavioralControlsPanel" Orientation="Vertical"
                    Margin="{x:Static ui:ChartUiDefaults.BehavioralControlsMargin}">
            <StackPanel x:Name="NormalizationControlsPanel" Orientation="Horizontal">
                <TextBlock Text="Normalization:"
                           VerticalAlignment="Center"
                           Margin="{x:Static ui:ChartUiDefaults.LabelMargin}" />

                <RadioButton x:Name="NormZeroToOneRadioControl"
                             Content="Zero-To-One"
                             Margin="{x:Static ui:ChartUiDefaults.RadioButtonMargin}"
                             GroupName="NormModes" />

                <RadioButton x:Name="NormPercentOfMaxRadioControl"
                             Content="% of Max"
                             Margin="{x:Static ui:ChartUiDefaults.RadioButtonMargin}"
                             GroupName="NormModes"
                             IsChecked="True" />

                <RadioButton x:Name="NormRelativeToMaxRadioControl"
                             Content="Relative to Max"
                             Margin="{x:Static ui:ChartUiDefaults.RadioButtonMargin}"
                             GroupName="NormModes" />
            </StackPanel>

            <StackPanel x:Name="NormalizedSubtypePanel" Orientation="Horizontal" Margin="0,6,0,0">
                <StackPanel x:Name="NormalizedPrimarySubtypePanel" Orientation="Vertical"
                            Margin="{x:Static ui:ChartUiDefaults.TransformPanelRightMargin}"
                            MinWidth="{x:Static ui:ChartUiDefaults.TransformPrimaryPanelMinWidth}">
                    <TextBlock Text="Primary Subtype:" FontWeight="Bold" Margin="0,0,0,5" />
                    <ComboBox x:Name="NormalizedPrimarySubtypeComboControl"
                              Width="{x:Static ui:ChartUiDefaults.SubtypeComboWidth}" />
                </StackPanel>

                <StackPanel x:Name="NormalizedSecondarySubtypePanelControl" Orientation="Vertical"
                            Margin="{x:Static ui:ChartUiDefaults.TransformPanelRightMargin}"
                            MinWidth="{x:Static ui:ChartUiDefaults.TransformSecondaryPanelMinWidth}"
                            Visibility="Collapsed">
                    <TextBlock Text="Secondary Subtype:" FontWeight="Bold" Margin="0,0,0,5" />
                    <ComboBox x:Name="NormalizedSecondarySubtypeComboControl"
                              Width="{x:Static ui:ChartUiDefaults.SubtypeComboWidth}" />
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <StackPanel x:Name="ChartContentPanelRoot" Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <lvc:CartesianChart x:Name="ChartNormControl"
                                    Grid.Column="0"
                                    LegendLocation="None"
                                    Zoom="{x:Static ui:ChartUiDefaults.DefaultZoom}"
                                    Pan="{x:Static ui:ChartUiDefaults.DefaultPan}"
                                    Hoverable="{x:Static ui:ChartUiDefaults.DefaultHoverable}"
                                    Margin="{x:Static ui:ChartUiDefaults.ChartContentMargin}"
                                    MinHeight="{x:Static ui:ChartUiDefaults.ChartMinHeight}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="{x:Static ui:ChartUiDefaults.AxisTitleTime}" />
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="{x:Static ui:ChartUiDefaults.AxisTitleRelative}"
                                  ShowLabels="True" />
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>

                <Border Grid.Column="1"
                        Background="#CC111111"
                        CornerRadius="4"
                        Padding="6"
                        Margin="0,10,10,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top">
                    <ItemsControl x:Name="NormalizedLegendItemsControl"
                                  HorizontalAlignment="Left">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ToggleButton IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                              Click="OnLegendItemToggle"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Padding="2"
                                              Margin="{Binding ItemMargin}"
                                              HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Left">
                                        <Rectangle Width="12"
                                                   Height="12"
                                                   Fill="{Binding Stroke}"
                                                   Stroke="White"
                                                   StrokeThickness="0.5"
                                                   Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Title}"
                                                   Foreground="White"
                                                   TextAlignment="Left" />
                                    </StackPanel>
                                </ToggleButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
